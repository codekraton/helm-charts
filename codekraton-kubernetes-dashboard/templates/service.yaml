apiVersion: v1
kind: Service
metadata:
  name: {{ include "codekraton-kubernetes-dashboard.fullname" . }}
  labels:
    {{- include "codekraton-kubernetes-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "codekraton-kubernetes-dashboard.selectorLabels" . | nindent 4 }}
